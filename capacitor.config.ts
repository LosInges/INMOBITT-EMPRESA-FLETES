import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'INMOBITT EMPRESA FLETES',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
